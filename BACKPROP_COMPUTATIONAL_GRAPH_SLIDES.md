---
marp: true
theme: default
paginate: true
---

# 5章 誤差逆伝播法

## 5.1 計算グラフ
## 5.1.1 計算グラフで解く

---

# この範囲で学ぶこと

- 誤差逆伝播法を理解する準備として、計算グラフを学ぶ
- 複雑な計算を、小さな計算の組み合わせとして表す
- 計算グラフを使って、計算の流れを視覚的に整理する
- 順伝播と逆伝播の考え方につなげる

---

# 誤差逆伝播法とは

ニューラルネットワークの学習では、損失を小さくするために重みを更新する。

そのためには、

```text
各重みを少し変えたとき、損失がどれだけ変わるか
```

を知る必要がある。

この「変化の大きさ」を効率よく求める方法が、誤差逆伝播法である。

---

# なぜ計算グラフを使うのか

計算グラフを使うと、計算全体を次のように分解できる。

```text
大きな計算
  -> 小さな計算
  -> 小さな計算
  -> 小さな計算
```

それぞれの小さな計算だけを見ればよいので、複雑な式でも扱いやすくなる。

---

# 計算グラフとは

計算グラフは、計算の流れをグラフで表したもの。

- ノード: 演算を表す
- エッジ: データの流れを表す
- 左から右へ計算を進める

例:

```text
100 ----\
        (*) ---- 200 ---- (*) ---- 220
2   ----/          tax 1.1
```

---

# 計算グラフで解く手順

1. 問題に出てくる値を確認する
2. 必要な計算を小さな演算に分ける
3. 演算をノードとして並べる
4. 左から右へ順番に計算する

計算グラフでは、各ノードは自分に入ってきた値だけを使って計算する。

---

# 例: りんごを買う計算

条件:

- りんご1個: 100円
- 個数: 2個
- 消費税: 1.1倍

計算:

```text
100 * 2 = 200
200 * 1.1 = 220
```

答えは 220円。

---

# 例を計算グラフで表す

```text
りんごの値段 100 ----\
                    (*) ---- 200 ----\
個数 2          ----/                 (*) ---- 220
                                      /
消費税 1.1 --------------------------/
```

このように、計算をノードごとに分けると、途中結果も含めて流れを確認できる。

---

# 順伝播

計算グラフで、左から右へ計算する流れを順伝播という。

```text
入力
  -> 計算
  -> 計算
  -> 出力
```

りんごの例では、

```text
100, 2, 1.1 -> 200 -> 220
```

という流れになる。

---

# 計算グラフの利点

- 計算の流れを視覚的に理解できる
- 複雑な計算を小さな演算に分けられる
- 各ノードは局所的な計算だけに集中できる
- 後で逆方向にたどることで、微分を効率よく計算できる

この最後の点が、誤差逆伝播法につながる。

---

# 逆伝播へのつながり

順伝播では、入力から出力を計算する。

```text
入力 -> 出力
```

逆伝播では、出力側から入力側へ影響度を戻していく。

```text
出力 -> 入力
```

つまり、計算グラフは「計算結果を出すため」だけでなく、「どの入力が結果にどれだけ影響したか」を調べるためにも使える。

---

# まとめ

- 計算グラフは、計算をノードとエッジで表す方法
- 複雑な計算を、小さな演算の組み合わせに分解できる
- 左から右へ計算する流れを順伝播という
- 計算グラフを逆向きにたどることで、誤差逆伝播法の考え方につながる

